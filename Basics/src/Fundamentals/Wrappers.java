package Fundamentals;

public class Wrappers {
  public static void main (String[] args) {
    // primitive types are not classes, so no behavior
    int num = 1;

    // wraps primitive types, gives behavior
    Integer num1 = 2;
  }
}
